version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm start
      artifacts:
        baseDirectory: app  # Répertoire où les artefacts de construction sont stockés
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
