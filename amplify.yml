version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "No preBuild step needed"
        build:
          commands:
            - echo "No build step needed"
      artifacts:
        baseDirectory: app  # Répertoire contenant les fichiers à déployer
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
