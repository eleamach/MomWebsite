version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "No preBuild step needed"
        build:
          commands:
            - echo "No build step needed"
      artifacts:
        baseDirectory: view  # Répertoire contenant les fichiers HTML
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
